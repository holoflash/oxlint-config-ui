This is the start of migrating to vite and npm.
Using the JSON provided by `oxlint --rules -f=json
I still believe that the best approach is to write something in Rust.
Either as a TUI or a native window (using GPUI?) but for now, I wanna see this version through and make it possible to install this as an npm package in a repo that has oxlint as a dependency.